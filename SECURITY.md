# Security

Report vulnerabilities privately through GitHub's security advisory workflow. Do not include secrets, private datasets, model credentials, or unsafe executable training artifacts in an issue.

The default labs are local and dependency-free. Real agent or RL environments must use least privilege, disposable state, resource limits, and an explicit cleanup procedure.
